# 天道玄机 — 环境变量示例
# ⚠️ 安全警告：切勿将真实密钥提交到 Git 仓库！
# 将本文件复制为 .env 并填入占位符以外的值仅限本地开发使用；
# 生产构建（vite build）检测到真实密钥会直接报错拦截，防止密钥被打包进产物并随部署泄露。

# GEMINI_API_KEY: 仅用于本地开发时注入 Gemini 密钥（供 import.meta.env.VITE_GEMINI_API_KEY 读取）。
# 生产环境请留空或使用占位符；真实密钥请在页面右上角【设置】面板中配置。
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# 其他模型服务商（OpenAI / DeepSeek / 通义千问 / 智谱清言 / 自定义 OpenAI 兼容接口）
# 无需在此配置，直接在页面右上角【设置】中填写 API Key 即可（仅保存在浏览器 localStorage，不会上传）。
